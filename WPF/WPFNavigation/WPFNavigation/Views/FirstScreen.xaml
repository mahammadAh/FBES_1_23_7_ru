<UserControl x:Class="WPFNavigation.Views.FirstScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFNavigation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="360">
    <StackPanel>
        <TextBox Height="40" Grid.Column="0" FontSize="15" Text="{Binding Text}"></TextBox>
       

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Height="40" Command="{Binding SendTextCommnad}" CommandParameter="Second">Send to Second</Button>
            <Button Grid.Column="1" Height="40" Command="{Binding SendTextCommnad}" CommandParameter="Third">Send to Third</Button>
        </Grid>
     
    </StackPanel>
</UserControl>
