<UserControl x:Class="WPFNavigation.Views.SecondScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFNavigation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="360">

    <Grid>
    <Grid.RowDefinitions>
        <RowDefinition></RowDefinition>
        <RowDefinition></RowDefinition>
        <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>


    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Height="40" Command="{Binding ChangeScreens}" CommandParameter="First">First Screen</Button>
        <Button Grid.Column="1" Height="40" Command="{Binding ChangeScreens}" CommandParameter="Third">Third Screen</Button>



    </Grid>

    </Grid>

</UserControl>
